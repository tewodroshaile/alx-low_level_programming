#include <stdio.h>
/**
 * main - the main function to return number of arguments
 * @argc: the first argument
 * @argv: the second argument
 * Return: returns the succuss of the program
 */
int main(int argc, char *argv[])
{
printf("%d\n", argc - 1);
(void)argv;
return (0);
}

