#include "main.h"
#include <string.h>
/**
 * _strcat - a function which concatinates two chars
 * @dest: the first string
 * @src: the second character
 *
 * Description: acept two collection of chars and print the concatinate
 * Return: returns the concatinated data
 */
char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
