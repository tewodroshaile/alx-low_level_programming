#include <stdio.h>
/**
 * main-the main function returns the sentence
 *
 * Return:0
 */
int main(void)
{
char word[50] = "\"Programming is like building a multilingual puzzle";
puts(word);
return (0);
}
